kind = "web"
previewPath = "/"
title = "Secure Election Management System"
version = "1.0.0"
id = "artifacts/election-system"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24363

[services.development]
run = "pnpm --filter @workspace/election-system run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/election-system", "run", "build" ]
publicDir = "artifacts/election-system/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24363"
BASE_PATH = "/"
